<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 60">
  <!-- Blue ring (top left) -->
  <circle cx="20" cy="20" r="12" fill="none" stroke="#0085c7" stroke-width="4"/>

  <!-- Yellow/Gold ring (top center) -->
  <circle cx="60" cy="20" r="12" fill="none" stroke="#ffd700" stroke-width="4"/>

  <!-- Black ring (top right) -->
  <circle cx="100" cy="20" r="12" fill="none" stroke="#000000" stroke-width="4"/>

  <!-- Green ring (bottom left) -->
  <circle cx="40" cy="45" r="12" fill="none" stroke="#00a651" stroke-width="4"/>

  <!-- Red ring (bottom right) -->
  <circle cx="80" cy="45" r="12" fill="none" stroke="#ee334e" stroke-width="4"/>
</svg>